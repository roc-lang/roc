[package]
name = "sized_str"
description = "Fixed-sized strings (Str4, Str8, Str16) that are zero-padded at the end."

authors.workspace = true
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]

[dev-dependencies]
