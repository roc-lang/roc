[package]
name = "can"
description = "Canonicalization (name resolution and interning strings)"

authors.workspace = true
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
arena = { path = "../../arena" }
intern = { path = "../../intern" }
