# +set function_kind=erased
# +emit:mono
app "test" provides [main] to "./platform"

f = \s ->
  if Bool.true
  then \{} -> ""
  #    ^^^^^^^^^ {}* -?-> Str
  else \{} -> s
  #    ^^^^^^^^ {}* -?-> Str

main = (f "") {}
#       ^^^^ {} -?-> Str

# -emit:mono
