app [doIt] { }

Effect : [
    DoIt {} ({} -> Effect),
]

Task := ({} -> Effect) -> Effect

doIt : {} -> Task
doIt = \{} ->
#^^^^{-1} {} -[[doIt(0)]]-> Task
    @Task \toNext ->
        DoIt {} \{} -> (toNext {})
