module []e = \r ->
    when t is
        E ->
            when g is
                P ->
                    if y then
                        e
                    else
                        # .t=
                        s
