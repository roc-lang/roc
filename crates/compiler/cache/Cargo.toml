[package]
name = "roc_cache"
description = "Caching for Roc IRs"

authors.workspace = true
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
bumpalo = "3.12.0" # no collections! This is intentional, because they store pointers to the arena.

[dev-dependencies]
pretty_assertions.workspace = true
