// TODO require a different .node file depending on my OS and arch,
// because presumably the extension will have shipped with more than one.
// (Maybe not though, if we can control that? e.g. similar to roc-npm?)
const ext = require("libroc_vscode.node");

// TODO register things using ext
