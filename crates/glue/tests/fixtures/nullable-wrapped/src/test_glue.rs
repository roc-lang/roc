// ⚠️ GENERATED CODE ⚠️ - this entire file was generated by the `roc glue` CLI command

#![allow(unused_unsafe)]
#![allow(unused_variables)]
#![allow(dead_code)]
#![allow(unused_mut)]
#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
#![allow(non_upper_case_globals)]
#![allow(clippy::undocumented_unsafe_blocks)]
#![allow(clippy::redundant_static_lifetimes)]
#![allow(clippy::unused_unit)]
#![allow(clippy::missing_safety_doc)]
#![allow(clippy::let_and_return)]
#![allow(clippy::missing_safety_doc)]
#![allow(clippy::redundant_static_lifetimes)]
#![allow(clippy::needless_borrow)]
#![allow(clippy::clone_on_copy)]

type Op_StderrWrite = roc_std::RocStr;
type Op_StdoutWrite = roc_std::RocStr;
type TODO_roc_function_69 = roc_std::RocStr;
type TODO_roc_function_70 = roc_std::RocStr;

#[cfg(any(
    target_arch = "arm",
    target_arch = "aarch64",
    target_arch = "wasm32",
    target_arch = "x86",
    target_arch = "x86_64"
))]
#[derive(Clone, Copy, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[repr(u8)]
pub enum discriminant_StrFingerTree {
    Empty = 0,
    More = 1,
    Single = 2,
}

impl core::fmt::Debug for discriminant_StrFingerTree {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Empty => f.write_str("discriminant_StrFingerTree::Empty"),
            Self::More => f.write_str("discriminant_StrFingerTree::More"),
            Self::Single => f.write_str("discriminant_StrFingerTree::Single"),
        }
    }
}

#[cfg(any(
    target_arch = "arm",
    target_arch = "aarch64",
    target_arch = "wasm32",
    target_arch = "x86",
    target_arch = "x86_64"
))]
#[repr(transparent)]
pub struct StrFingerTree {
    pointer: *mut union_StrFingerTree,
}


#[cfg(any(
    target_arch = "arm",
    target_arch = "aarch64",
    target_arch = "wasm32",
    target_arch = "x86",
    target_arch = "x86_64"
))]
#[repr(C)]
union union_StrFingerTree {
    More: core::mem::ManuallyDrop<StrFingerTree_More>,
    Single: core::mem::ManuallyDrop<StrFingerTree_Single>,
    _sizer: [u8; 8],
}

#[cfg(any(
    target_arch = "arm",
    target_arch = "aarch64",
    target_arch = "wasm32",
    target_arch = "x86",
    target_arch = "x86_64"
))]
#[derive(Clone, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[repr(transparent)]
struct StrFingerTree_Single {
    pub f0: roc_std::RocStr,
}

#[cfg(any(
    target_arch = "arm",
    target_arch = "aarch64",
    target_arch = "wasm32",
    target_arch = "x86",
    target_arch = "x86_64"
))]
#[derive(Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[repr(C)]
struct StrFingerTree_More {
    pub f0: roc_std::RocStr,
    pub f1: StrFingerTree,
}

impl StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    #[inline(always)]
    fn storage(&self) -> Option<&core::cell::Cell<roc_std::Storage>> {
        let mask = match std::mem::size_of::<usize>() {
            4 => 0b11,
            8 => 0b111,
            _ => unreachable!(),
        };

        // NOTE: pointer provenance is probably lost here
        let unmasked_address = (self.pointer as usize) & !mask;
        let untagged = unmasked_address as *const core::cell::Cell<roc_std::Storage>;

        if untagged.is_null() {
            None
        } else {
            unsafe {
                Some(&*untagged.sub(1))
            }
        }
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Returns which variant this tag union holds. Note that this never includes a payload!
    pub fn discriminant(&self) -> discriminant_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the recursive pointer
        unsafe { core::mem::transmute::<u8, discriminant_StrFingerTree>((self.pointer as u8) & 0b11) }
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Internal helper
    fn tag_discriminant(pointer: *mut union_StrFingerTree, discriminant: discriminant_StrFingerTree) -> *mut union_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the union pointer
        let untagged = (pointer as usize) & (!0b11 as usize);
        let tagged = untagged | (discriminant as usize);

        tagged as *mut union_StrFingerTree
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Internal helper
    fn union_pointer(&self) -> *mut union_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the union pointer
        ((self.pointer as usize) & (!0b11 as usize)) as *mut union_StrFingerTree
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    /// A tag named Empty, which has no payload.
    pub const Empty: Self = Self {
        pointer: core::ptr::null_mut(),
    };

    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    /// Construct a tag named `More`, with the appropriate payload
    pub fn More(arg0: roc_std::RocStr, arg1: StrFingerTree) -> Self {
            let size = core::mem::size_of::<union_StrFingerTree>();
            let align = core::mem::align_of::<union_StrFingerTree>() as u32;

            unsafe {
                let ptr = roc_std::roc_alloc_refcounted::<union_StrFingerTree>();

                *ptr = union_StrFingerTree {
                    More: core::mem::ManuallyDrop::new(StrFingerTree_More {
                    f0: arg0,
                    f1: arg1,
                })
                };

                Self {
                    pointer: Self::tag_discriminant(ptr, discriminant_StrFingerTree::More),
                }
            }
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `More` and convert it to `More`'s payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `More`.
            pub unsafe fn into_More(mut self) -> (roc_std::RocStr, StrFingerTree) {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::More);
        let payload = {
            let ptr = (self.pointer as usize & !0b11) as *mut union_StrFingerTree;
            let mut uninitialized = core::mem::MaybeUninit::uninit();
            let swapped = unsafe {
                core::mem::replace(
                    &mut (*ptr).More,
                    core::mem::ManuallyDrop::new(uninitialized.assume_init()),
                )
            };

            core::mem::forget(self);

            core::mem::ManuallyDrop::into_inner(swapped)
        };

        (
            payload.f0, 
            payload.f1
        )
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `More` and return its payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `More`.
            pub unsafe fn as_More(&self) -> (&roc_std::RocStr, &StrFingerTree) {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::More);
        let payload = {
            let ptr = (self.pointer as usize & !0b11) as *mut union_StrFingerTree;

            unsafe { &(*ptr).More }
        };

        (
            &payload.f0, 
            &payload.f1
        )
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    /// Construct a tag named `Single`, with the appropriate payload
    pub fn Single(arg0: roc_std::RocStr) -> Self {
            let size = core::mem::size_of::<union_StrFingerTree>();
            let align = core::mem::align_of::<union_StrFingerTree>() as u32;

            unsafe {
                let ptr = roc_std::roc_alloc_refcounted::<union_StrFingerTree>();

                *ptr = union_StrFingerTree {
                    Single: core::mem::ManuallyDrop::new(StrFingerTree_Single {
                    f0: arg0,
                })
                };

                Self {
                    pointer: Self::tag_discriminant(ptr, discriminant_StrFingerTree::Single),
                }
            }
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `Single` and convert it to `Single`'s payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `Single`.
            pub unsafe fn into_Single(mut self) -> roc_std::RocStr {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::Single);
        let payload = {
            let ptr = (self.pointer as usize & !0b11) as *mut union_StrFingerTree;
            let mut uninitialized = core::mem::MaybeUninit::uninit();
            let swapped = unsafe {
                core::mem::replace(
                    &mut (*ptr).Single,
                    core::mem::ManuallyDrop::new(uninitialized.assume_init()),
                )
            };

            core::mem::forget(self);

            core::mem::ManuallyDrop::into_inner(swapped)
        };

        
        payload.f0
    }

    #[cfg(any(
        target_arch = "arm",
        target_arch = "wasm32",
        target_arch = "x86"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `Single` and return its payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `Single`.
            pub unsafe fn as_Single(&self) -> &roc_std::RocStr {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::Single);
        let payload = {
            let ptr = (self.pointer as usize & !0b11) as *mut union_StrFingerTree;

            unsafe { &(*ptr).Single }
        };

        
        &payload.f0
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Returns which variant this tag union holds. Note that this never includes a payload!
    pub fn discriminant(&self) -> discriminant_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the recursive pointer
        unsafe { core::mem::transmute::<u8, discriminant_StrFingerTree>((self.pointer as u8) & 0b111) }
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Internal helper
    fn tag_discriminant(pointer: *mut union_StrFingerTree, discriminant: discriminant_StrFingerTree) -> *mut union_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the union pointer
        let untagged = (pointer as usize) & (!0b111 as usize);
        let tagged = untagged | (discriminant as usize);

        tagged as *mut union_StrFingerTree
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Internal helper
    fn union_pointer(&self) -> *mut union_StrFingerTree {
        // The discriminant is stored in the unused bytes at the end of the union pointer
        ((self.pointer as usize) & (!0b111 as usize)) as *mut union_StrFingerTree
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `More` and convert it to `More`'s payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `More`.
            pub unsafe fn into_More(mut self) -> (roc_std::RocStr, StrFingerTree) {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::More);
        let payload = {
            let ptr = (self.pointer as usize & !0b111) as *mut union_StrFingerTree;
            let mut uninitialized = core::mem::MaybeUninit::uninit();
            let swapped = unsafe {
                core::mem::replace(
                    &mut (*ptr).More,
                    core::mem::ManuallyDrop::new(uninitialized.assume_init()),
                )
            };

            core::mem::forget(self);

            core::mem::ManuallyDrop::into_inner(swapped)
        };

        (
            payload.f0, 
            payload.f1
        )
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `More` and return its payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `More`.
            pub unsafe fn as_More(&self) -> (&roc_std::RocStr, &StrFingerTree) {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::More);
        let payload = {
            let ptr = (self.pointer as usize & !0b111) as *mut union_StrFingerTree;

            unsafe { &(*ptr).More }
        };

        (
            &payload.f0, 
            &payload.f1
        )
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `Single` and convert it to `Single`'s payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `Single`.
            pub unsafe fn into_Single(mut self) -> roc_std::RocStr {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::Single);
        let payload = {
            let ptr = (self.pointer as usize & !0b111) as *mut union_StrFingerTree;
            let mut uninitialized = core::mem::MaybeUninit::uninit();
            let swapped = unsafe {
                core::mem::replace(
                    &mut (*ptr).Single,
                    core::mem::ManuallyDrop::new(uninitialized.assume_init()),
                )
            };

            core::mem::forget(self);

            core::mem::ManuallyDrop::into_inner(swapped)
        };

        
        payload.f0
    }

    #[cfg(any(
        target_arch = "aarch64",
        target_arch = "x86_64"
    ))]
    /// Unsafely assume this `StrFingerTree` has a `.discriminant()` of `Single` and return its payload.
            /// (Always examine `.discriminant()` first to make sure this is the correct variant!)
            /// Panics in debug builds if the `.discriminant()` doesn't return `Single`.
            pub unsafe fn as_Single(&self) -> &roc_std::RocStr {
                debug_assert_eq!(self.discriminant(), discriminant_StrFingerTree::Single);
        let payload = {
            let ptr = (self.pointer as usize & !0b111) as *mut union_StrFingerTree;

            unsafe { &(*ptr).Single }
        };

        
        &payload.f0
    }
}

impl Drop for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn drop(&mut self) {
        // We only need to do any work if there's actually a heap-allocated payload.
        if let Some(storage) = self.storage() {
            let mut new_storage = storage.get();

            // Decrement the refcount
            let needs_dealloc = !new_storage.is_readonly() && new_storage.decrease();

            if needs_dealloc {
                // Drop the payload first.
                            match self.discriminant() {
                discriminant_StrFingerTree::Empty => {}
                discriminant_StrFingerTree::More => unsafe { core::mem::ManuallyDrop::drop(&mut (&mut *self.union_pointer()).More) },
                discriminant_StrFingerTree::Single => unsafe { core::mem::ManuallyDrop::drop(&mut (&mut *self.union_pointer()).Single) },
            }


                // Dealloc the pointer
                let alignment = core::mem::align_of::<Self>().max(core::mem::align_of::<roc_std::Storage>());

                unsafe { crate::roc_dealloc(storage.as_ptr().cast(), alignment as u32); }
            } else {
                // Write the storage back.
                storage.set(new_storage);
            }
        }
    }
}

impl Eq for StrFingerTree {}

impl PartialEq for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn eq(&self, other: &Self) -> bool {
            if self.discriminant() != other.discriminant() {
                return false;
            }

            unsafe {
            match self.discriminant() {
                discriminant_StrFingerTree::Empty => true,
                discriminant_StrFingerTree::More => (&*self.union_pointer()).More == (&*other.union_pointer()).More,
                discriminant_StrFingerTree::Single => (&*self.union_pointer()).Single == (&*other.union_pointer()).Single,
            }
        }
    }
}

impl PartialOrd for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn partial_cmp(&self, other: &Self) -> Option<core::cmp::Ordering> {
        match self.discriminant().partial_cmp(&other.discriminant()) {
            Some(core::cmp::Ordering::Equal) => {}
            not_eq => return not_eq,
        }

        unsafe {
            match self.discriminant() {
                discriminant_StrFingerTree::Empty => Some(core::cmp::Ordering::Equal),
                discriminant_StrFingerTree::More => (&*self.union_pointer()).More.partial_cmp(&(&*other.union_pointer()).More),
                discriminant_StrFingerTree::Single => (&*self.union_pointer()).Single.partial_cmp(&(&*other.union_pointer()).Single),
            }
        }
    }
}

impl Ord for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn cmp(&self, other: &Self) -> core::cmp::Ordering {
            match self.discriminant().cmp(&other.discriminant()) {
                core::cmp::Ordering::Equal => {}
                not_eq => return not_eq,
            }

            unsafe {
            match self.discriminant() {
                discriminant_StrFingerTree::Empty => core::cmp::Ordering::Equal,
                discriminant_StrFingerTree::More => (&*self.union_pointer()).More.cmp(&(&*other.union_pointer()).More),
                discriminant_StrFingerTree::Single => (&*self.union_pointer()).Single.cmp(&(&*other.union_pointer()).Single),
            }
        }
    }
}

impl Clone for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn clone(&self) -> Self {
        if let Some(storage) = self.storage() {
            let mut new_storage = storage.get();
            if !new_storage.is_readonly() {
                new_storage.increment_reference_count();
                storage.set(new_storage);
            }
        }

        Self {
            pointer: self.pointer
        }
    }
}

impl core::hash::Hash for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn hash<H: core::hash::Hasher>(&self, state: &mut H) {        match self.discriminant() {
            discriminant_StrFingerTree::Empty => discriminant_StrFingerTree::Empty.hash(state),
            discriminant_StrFingerTree::More => unsafe {
                    discriminant_StrFingerTree::More.hash(state);
                    (&*self.union_pointer()).More.hash(state);
                },
            discriminant_StrFingerTree::Single => unsafe {
                    discriminant_StrFingerTree::Single.hash(state);
                    (&*self.union_pointer()).Single.hash(state);
                },
        }
    }
}

impl core::fmt::Debug for StrFingerTree {
    #[cfg(any(
        target_arch = "arm",
        target_arch = "aarch64",
        target_arch = "wasm32",
        target_arch = "x86",
        target_arch = "x86_64"
    ))]
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str("StrFingerTree::")?;

        unsafe {
            match self.discriminant() {
                discriminant_StrFingerTree::Empty => f.write_str("Empty"),
                discriminant_StrFingerTree::More => f.debug_tuple("More")
        .field(&(&*(&*self.union_pointer()).More).f0)
.field(&(&*(&*self.union_pointer()).More).f1)
        .finish(),
                discriminant_StrFingerTree::Single => f.debug_tuple("Single")
        .field(&(&*(&*self.union_pointer()).Single).f0)
        .finish(),
            }
        }
    }
}
