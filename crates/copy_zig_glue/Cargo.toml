[package]
name = "copy_zig_glue"
description = "This crate Copies the glue source files for zig platforms from the roc builtins this is only temporary, we should create a zig package for the test platforms"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
roc_test_utils_dir = { path = "../test_utils_dir" }
