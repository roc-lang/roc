## Type ids of a function's arguments and result: what a provided procedure's
## exported symbol takes and returns, or what a host passes to and receives
## from a stored callable.
FunctionSignature := { args : List(U64), ret : U64 }
