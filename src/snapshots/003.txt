~~~META
description=Import statement
~~~SOURCE

modAle

[decoder]

import json.Json

decoder = Utf8.decode
~~~PARSE
(file
    (malformed 'missing_header')
    (ident '' 'modAle')
    (list
        (ident '' 'decoder'))
    (import
        'json'
        '.Json'
        '')
    (decl
        (ident 'decoder')
        (ident 'Utf8' '.decode')))