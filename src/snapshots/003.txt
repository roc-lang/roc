~~~META
description=Import statement
~~~SOURCE
<malformed_header>

modAle

[decoder]

import json.Json

decoder = Utf8.decode
~~~PARSE
(file
    (malformed)
    (ident '' 'modAle')
    (list
        (ident '' 'decoder'))
    (import
        'json'
        '.Json'
        '')
    (decl
        (ident 'decoder')
        (ident 'Utf8' '.decode')))