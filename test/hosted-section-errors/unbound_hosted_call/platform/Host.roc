## Hosted functions for the unbound-hosted-call fixture. `vanish!` is
## deliberately absent from the platform header's hosted section.
Host := [].{
    double! : I64 => I64
    vanish! : I64 => I64
}
